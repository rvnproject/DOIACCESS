export const currentUrl = state => state.current_url;

export const currentTabId = state => state.current_tab_id;

export const baseUrlByIdType = state => state.base_url_by_id_type;

export const stateByUrl = state => state.state_by_url;

export const lockedUrlState = state => state.locked_url_state;

export const isVuexLoaded = state => state.is_vuex_loaded;

export const doiCountByTabId = state => state.doi_count_by_tab_id;

export const errorMessage = state => state.error_message;

export const isHighlighted = state => state.is_highlighted;

export const highlightColor = state => state.highlight_color;

export const fontColor = state => state.font_color;